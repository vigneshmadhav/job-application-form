{
  "version": 2,
  "build": {
    "env": {
      "NPM_FLAGS": "--legacy-peer-deps"
    },
    "command": "npm run build",
    "outputDirectory": "dist"
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}